# Task 2: Project Scaffolding (Template)

Objective: create structure, clone templates, rename services, init git.

Template Steps (fill from requirements)
- Create root directory structure (protocols + each service).
- Clone microservice template for each service; run `make rename NEW_NAME=<service-name>`.
- Clone protocols template into protocols repo; init git; add .gitignore.
- For each service: init git, update module path/imports as needed, verify `make build` or `go test ./...`.

Verification
- Dirs created; repos initialized; rename applied; builds/tests pass.

Confirmation required: present plan/output and await user before proceeding.
